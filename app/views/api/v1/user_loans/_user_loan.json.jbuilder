json.extract! user_loan, :id, :country_id, :loan_id, :user_id, :currency_id, :originator_id, :user_account_id, :platform_id, :slice_name, :market, :xirr, :investment_amount, :invest_mode, :position, :date_in, :date_out, :principal_remaining, :interest, :bonus, :tax, :loss, :fee, :contract_url
#json.url user_loan_url(user_loan, format: :json)
