json.extract! user_loan, :id,  :loan_id,  :user_account_id, :slice_name, :market, :xirr, :investment_amount, :invest_mode, :position, :date_in, :date_out
#json.url user_loan_url(user_loan, format: :json)
